% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/covariate_selection.R
\name{make_spatial_folds}
\alias{make_spatial_folds}
\title{Spatial-block cross-validation folds (SPCV)}
\usage{
make_spatial_folds(
  data,
  response,
  candidates,
  coords = c("lon", "lat"),
  k = 10,
  metric = "haversine",
  threshold = NULL,
  crs = NULL,
  linkage = "average",
  pca_var = 0.95,
  seed = 42,
  verbose = TRUE
)
}
\arguments{
\item{data}{A data.frame with the coordinate, response, and covariate columns.}

\item{response}{Name of the response column (block-mean view for stage 2).}

\item{candidates}{Character vector of covariate names (numeric ones feed the
stage-2 covariate view; others are ignored).}

\item{coords}{Length-2 character vector naming the two coordinate columns
(x/longitude then y/latitude). Default \code{c("lon", "lat")}. For
\code{metric = "haversine"} these must be lon/lat degrees; for
\code{metric = "euclidean"} with \code{crs = NULL} they are taken as already-planar
coordinates.}

\item{k}{Number of folds.}

\item{metric}{\code{"haversine"} (great-circle distance on lon/lat degrees; no
projection, valid anywhere on the globe) or \code{"euclidean"} (planar
distance; see \code{crs}). Euclidean uses the C-optimised \code{\link[stats:dist]{stats::dist()}} and
scales better to large n than the haversine matrix.}

\item{threshold}{AHC cut height: degrees for haversine, projected units
(e.g. metres) for euclidean. \code{NULL} (default) uses the 10th percentile of
pairwise distances.}

\item{crs}{Only used when \code{metric = "euclidean"}. \code{NULL} (default) treats
the \code{coords} columns as already projected. Otherwise an sf/PROJ CRS (a
proj string or EPSG code) used to reproject lon/lat to a planar system
before computing distances (needs the sf package). No projection is
assumed by default, so this function makes no study-area assumption.}

\item{linkage}{AHC linkage method; \code{"ward"} is downgraded to \code{"average"}
for haversine, where it is invalid.}

\item{pca_var}{Fraction of variance retained when the stage-2 covariate
view is compressed by PCA.}

\item{seed}{Random seed for the k-means / spectral steps.}

\item{verbose}{Print a one-line fold summary.}
}
\value{
An integer vector of fold ids (1..k), one per row of \code{data}.
}
\description{
Two-stage spatial CV (Wang et al. 2023), a faithful R port of the h2ml
\code{SPCVSplitter}. Stage 1 builds spatially coherent blocks by agglomerative
hierarchical clustering on the coordinates, cut at a distance threshold.
Stage 2 groups the blocks into \code{k} folds by a cluster ensemble (HBGF):
per-block means of location, covariates, and response are each k-means
clustered, the one-hot memberships are stacked into a co-occurrence
affinity, and spectral clustering assigns folds. Folds are geographically
separated and representative of covariate and label space.
}
\seealso{
\code{\link[=make_stratified_folds]{make_stratified_folds()}} for the non-spatial alternative;
\code{\link[=select_gam_covariates]{select_gam_covariates()}}, which builds these folds internally.
}

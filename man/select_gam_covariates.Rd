% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/covariate_selection.R
\name{select_gam_covariates}
\alias{select_gam_covariates}
\title{Automated GAM covariate validation and selection for mgcv}
\usage{
select_gam_covariates(
  data,
  response,
  candidates,
  structural = c("s(month, bs='cc')", "s(year, bs='re')", "te(lon, lat)"),
  coords = c("lon", "lat"),
  family = "auto",
  concurvity_max = 0.8,
  spearman_pre = 0.7,
  na_max = 0.3,
  cv_k = 5,
  cv_scheme = "spatial",
  cv_metric = "haversine",
  cv_crs = NULL,
  cv_threshold = NULL,
  cv_linkage = "ward",
  knots = NULL,
  offset = NULL,
  outdir = ".",
  logfile = "covariate_selection_log.txt",
  seed = 1,
  verbose = TRUE
)
}
\arguments{
\item{data}{A data.frame with the response, coordinates, candidate
covariates, and any variables used by \code{structural} / \code{offset}.}

\item{response}{Name of the response column.}

\item{candidates}{Character vector of candidate covariate names. Each enters
the model as a \verb{s(<name>)} smooth.}

\item{structural}{Character vector of mgcv terms forced into every model
and never screened, e.g. \code{c("s(year, bs='re')", "te(lon, lat)")}.}

\item{coords}{Length-2 character vector naming the longitude and latitude
columns, used for spatial folds and complete-case filtering.}

\item{family}{\code{"auto"} (default) profiles the response and ranks candidate
families by AIC + residual diagnostics on the structural-only model.
Alternatively a family object or zero-argument family generator to skip
the suggestion step.}

\item{concurvity_max}{Reject a candidate whose worst pairwise concurvity
against the already-accepted covariate smooths exceeds this (structural
terms excluded from the gate).}

\item{spearman_pre}{\verb{|Spearman rho|} threshold for the correlation clusters
reported in the decision table (annotation only; the concurvity gate does
the actual removal).}

\item{na_max}{Drop candidates whose NA/non-finite fraction exceeds this in
the univariate screen.}

\item{cv_k}{Number of CV folds.}

\item{cv_scheme}{Fold construction: \code{"spatial"} (SPCV, the default; see
\code{\link[=make_spatial_folds]{make_spatial_folds()}}) or \code{"stratified"} (non-spatial k-fold stratified
on the response; see \code{\link[=make_stratified_folds]{make_stratified_folds()}}). Use \code{"stratified"} for
low-prevalence (e.g. binomial presence/absence) targets where spatial
blocks would leave folds with too few positives.}

\item{cv_metric}{SPCV distance: \code{"haversine"} (the default; great-circle on
lon/lat degrees, correct anywhere with no projection) or \code{"euclidean"}
(planar; faster on large n, but see \code{cv_crs}).}

\item{cv_crs}{Only used when \code{cv_metric = "euclidean"}. \code{NULL} (default)
would compute euclidean distance on raw lon/lat degrees, which is only
valid near the equator -- so for euclidean pass an sf/PROJ CRS (proj
string or EPSG code) suited to your study area to reproject first (needs
the sf package). No study-area projection is assumed; \code{"haversine"} avoids
the question entirely.}

\item{cv_threshold}{SPCV AHC block threshold (\code{NULL} = 10th percentile of
pairwise distances).}

\item{cv_linkage}{SPCV AHC linkage (\code{"ward"} downgraded to \code{"average"} for
haversine).}

\item{knots}{mgcv per-smooth knot positions, passed to every \code{gam()}/\code{bam()}
fit. Mainly the cyclic boundary for \code{s(month, bs='cc')}: pass
\code{list(month = c(0.5, 12.5))} so the 12-month cycle wraps (Dec to Jan)
instead of collapsing months 1 and 12 onto the same knot. \code{NULL} lets
mgcv place knots automatically; entries for absent variables are ignored.}

\item{offset}{Name of an effort column; adds \verb{offset(log(<offset>))} to
every fit (including family suggestion, so AIC stays comparable).}

\item{outdir}{Directory for the decision-table CSV, diagnostic PNGs, and the
run log (created if absent).}

\item{logfile}{Filename (within \code{outdir}) for a run log that captures all
the console output; \code{NULL} disables it. Only written when \code{verbose = TRUE}
(there is nothing to capture otherwise). The sink is unwound on exit, so an
error mid-run still restores the console.}

\item{seed}{Random seed for fold construction.}

\item{verbose}{Print progress, the family table, the selection path, and
the final model summary.}
}
\value{
(Invisibly) a list: \code{model} (the final mgcv gam), \code{kept} (selected
covariate names), \code{family} (chosen family label), \code{decision} (per-candidate
decision table, also written to \code{covariate_decision_table.csv}), \code{path}
(forward-selection steps), \code{formula} (final formula as a string),
\code{dev_expl} (deviance explained), \code{folds} (integer fold vector), and
\code{logfile} (path to the run log, or \code{NULL} if none was written).

Side effects in \code{outdir}: \code{covariate_decision_table.csv},
\code{selection_diagnostics.png} (gam.check panels), \code{partial_effects.png},
and (unless disabled) the run log named by \code{logfile}.
}
\description{
Given a response and a pool of candidate covariates, this:
\enumerate{
\item profiles the response and auto-suggests a family (count / proportion /
positive / gaussian branches), ranked by AIC + residual diagnostics;
\item forward-selects a decorrelated, prediction-relevant covariate subset,
scored by cross-validated held-out deviance (spatial-block or
response-stratified folds), with a concurvity gate and a BIC tie-break /
1-SE stopping rule;
\item runs a bidirectional backward re-check (BIC), a final \code{select = TRUE}
shrinkage fit, and adequacy diagnostics.
}
}
\details{
Every threshold is a logged, overridable argument; nothing is hardcoded to
a particular dataset. Structural (spatial/temporal) terms are forced in and
not screened -- covariates must earn their place over and above them.
Random-effect structural terms (\code{bs = "re"}) are excluded from held-out CV
prediction, matching a deployment \code{predict(..., exclude = ...)}.

Search fits use \code{mgcv::bam(discrete = TRUE)} with the family shape
parameter (Tweedie p / negbin theta) fixed at its structural-only estimate
for speed; the final fit is a free \code{mgcv::gam(method = "REML")}.
}
\examples{
\donttest{
set.seed(1)
n  <- 400
df <- data.frame(lon = runif(n, -10, 0), lat = runif(n, 35, 45),
                 x1 = rnorm(n), x2 = rnorm(n))
df$y <- sin(df$x1) + 0.1 * df$lon + rnorm(n, sd = 0.3)
res <- select_gam_covariates(
  data       = df,
  response   = "y",
  candidates = c("x1", "x2"),
  structural = "te(lon, lat)",
  cv_k       = 3,
  cv_scheme  = "stratified",
  outdir     = tempdir()
)
res$kept
}

}
